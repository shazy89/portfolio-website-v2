import React from 'react';
import styled, { useTheme } from 'styled-components';
import { DividedScreen } from './dividedScreen';

export const DisplayProjects = () => {
  return <DividedScreen></DividedScreen>;
};
